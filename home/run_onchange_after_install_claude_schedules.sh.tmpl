#!/bin/zsh
# launchd plist の bootstrap を chezmoi apply 時に再実行する
# 以下のハッシュが変わったときに再実行される
# ob-daily-log.plist hash: {{ include "private_Library/private_LaunchAgents/com.claude.schedule.ob-daily-log.plist.tmpl" | sha256sum }}
# ob-daily-log run-scheduler.sh hash: {{ include "dot_claude/schedules/ob-daily-log/executable_run-scheduler.sh" | sha256sum }}

set -u

if [ -n "${CI:-}" ]; then
  echo "[claude-schedules] skip on CI"
  exit 0
fi

UID_NUM="$(id -u)"
DOMAIN="gui/$UID_NUM"

for plist in "$HOME/Library/LaunchAgents/com.claude.schedule."*.plist; do
  [ -f "$plist" ] || continue
  label="$(basename "$plist" .plist)"
  echo "[claude-schedules] reload: $label"
  launchctl bootout "$DOMAIN/$label" 2>/dev/null || true
  launchctl bootstrap "$DOMAIN" "$plist" || echo "[claude-schedules] bootstrap failed: $label (continuing)"
  launchctl enable "$DOMAIN/$label" 2>/dev/null || true
done
